<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.laserpay.pdei</groupId>
    <artifactId>pdei-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>platform-persistence</artifactId>
  <packaging>jar</packaging>

  <name>platform-persistence</name>
  <description>
    PDEI persistence layer (docs/SHARED-LIBRARY-API.md section 2, docs/PLATFORM-CONTRACT.md section 5):
    the Flyway-owned `pdei` PostgreSQL schema, the JPA entities that map it, Spring Data
    repositories, and the autoconfiguration that wires both into every service module.
    Library module: it is NOT repackaged into an executable jar.
  </description>

  <dependencies>
    <!-- Shared frozen API surface: Money, domain enums, event enums, id prefixes. -->
    <dependency>
      <groupId>com.laserpay.pdei</groupId>
      <artifactId>platform-common</artifactId>
    </dependency>

    <!-- JPA + Hibernate + HikariCP + spring-boot-autoconfigure. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <!--
      Jackson is the Hibernate JSON FormatMapper for @JdbcTypeCode(SqlTypes.JSON) columns.
      It arrives transitively through platform-common; declared explicitly because the JSON
      column mapping in this module hard-depends on it being present at runtime.
    -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- Schema ownership: this module ships db/migration and therefore owns Flyway. -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
